# from django.db import models

# # Create your models here. 
# # Each RESPONSE attribute should map to a database column
# # After updating this file, from the "backend\survivor" folder, run "python manage.py makemigrations survivor_app"
# class LoginResponse(models.Model):
#     login_successful = models.BooleanField(default=False)
#     message = models.CharField(max_length=100, blank=False, default='')